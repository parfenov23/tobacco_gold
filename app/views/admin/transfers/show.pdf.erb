<html>
<head>
  <meta charset='utf-8' />
</head>
<body>
  <style>
  table { 
    width: 100%; /* Ширина таблицы */
    /*        border: 1px solid black;*/ /* Рамка вокруг таблицы */
    border-collapse: collapse; /* Отображать только одинарные линии */
    margin-top: 16px;
    border-bottom: 1px solid #fff;
  }
  th { 
    text-align: left; /* Выравнивание по левому краю */
    background: #ccc; /* Цвет фона ячеек */
    padding: 5px; /* Поля вокруг содержимого ячеек */
    border: 1px solid black; /* Граница вокруг ячеек */
    text-align: center;
  }
  td { 
    padding: 5px; /* Поля вокруг содержимого ячеек */
    border: 1px solid black; /* Граница вокруг ячеек */
  }
  td.noBorder{
    border: initial;
  }
  td.text_center{
    text-align: center;
  }
  td.text_right{
    text-align: right;
  }
  thead { display: table-header-group }
  tfoot { display: table-row-group }
  tr { page-break-inside: avoid }
</style>
<div class="well">
<!--   <div>
    <b>Название:</b>
    <span><%= current_magazine.title %></span>
  </div>
  <div>
    <b>Адрес:</b>
    <span><%= current_magazine.address %></span>
  </div> -->
  <div style="text-align: center; margin-top: 24px;">
    <% date_sale = @transfer.created_at %>
    <b>Трансфер накладная № <%= @transfer.id %> от <%= date_sale.strftime("%d.%m.%Y") %></b>
  </div>
  <table class="table">
    <thead>
      <tr>
        <th>№</th>
        <th>Товар</th>
        <th>Ед.</th>
        <th>Кол-во</th>
        <th>Цена</th>
        <th>Сумма</th>
      </tr>
    </thead>
    <tbody>
      <% num = 0 %>
      <% @transfer.transfer_items.each do |transfer_item| %>
        <tr>
          <td><%= num+=1 %></td>
          <td><%= transfer_item.product_item.product.title %> - <%= transfer_item.product_item.title %></td>
          <td class="text_center">шт</td>
          <td class="text_right"><%= transfer_item.count %></td>
          <td class="text_right"><%= transfer_item.price %></td>
          <td class="text_right"><%= transfer_item.count * transfer_item.price %></td>
        </tr>
      <% end %>
      <tr>
        <td class="noBorder"></td>
        <td class="noBorder"></td>
        <td class="noBorder"></td>
        <td class="noBorder"></td>
        <td class="noBorder text_right">Итого:</td>
        <td class="text_right"><%= @transfer.sum %></td>
      </tr>
    </tbody>
  </table>
  <div style="margin-top: 24px;"> 
    Всего наименований <%= @transfer.transfer_items.count %>, на сумму <%= @transfer.sum %> руб. 
  </div>
  <div>
    <div style="margin-top: 24px;">
      Отпустил
      <span>______________</span>
      <span style="margin-left: 14px; text-decoration: underline;">&nbsp;&nbsp;Склад&nbsp;&nbsp;</span>
      <span style="margin-left: 14px; text-decoration: underline;">&nbsp;&nbsp;<%= (@transfer.magazine_from.title rescue nil)%>&nbsp;&nbsp;</span>
    </div>
  </div>
</div>
</body>
</html>