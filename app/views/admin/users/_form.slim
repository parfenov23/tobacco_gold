- action_url = @model.id.nil? ? "/admin/users" : "/admin/users/#{@model.id}"
- method = @model.id.nil? ? "post" : "put"
form.form-horizontal action="#{action_url}" method="post" enctype="multipart/form-data"
  .form-group
    |Email:
    input.form-control name="users[email]" type="text" value="#{@model.email}"
  .form-group
    |Логин:
    input.form-control name="users[login]" type="text" value="#{@model.login}"
  .form-group
    |Пароль(6):
    input.form-control name="users[password]" type="text"
  .form-group
    |Процент от продаж:
    input.form-control name="users[procent_sale]" type="text" value=@model.procent_sale
  .form-group
    |Выход на смену:
    input.form-control name="users[sum_shift]" type="text" value=@model.sum_shift
  .form-group
    |Права:
    = select_tag("users[role]", options_for_select([["Администратор", "admin"], ["Менеджер", "manager"]], @model.role.to_s), {:class => 'form-control'})
  .form-group
    |Магазин:
    = select_tag("users[magazine_id]", options_for_select(current_company.magazines.map{|magaz| [magaz.title, magaz.id]}, @model.magazine_id), {:class => 'form-control'})
  input type="hidden" name="_method" value="#{method}"
  button.btn.btn-primary-material.submit type="submit"  Сохранить